package com.example.ejemplosecurity2.security;


import org.springframework.context.annotation.Configuration;

@Configuration
public class SecurityConfig {

    /*
     @Bean
    public AuthenticationManager authManager(HttpSecurity http, PasswordEncoderConfig passwordEncoderConfig, UserDetailsService userDetailsService) throws Exception {
        return http.getSharedObject(AuthenticationManagerBuilder.class)
                .userDetailsService(userDetailsService).passwordEncoder(passwordEncoderConfig.passwordEncoder()).
    }
    * */

}